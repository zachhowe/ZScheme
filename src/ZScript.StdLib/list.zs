;; list.zs — List operations
(module list)

;; Map a function over a list
;; (define (map [f : (Fn [a] b)] [xs : (List a)]) : (List b) ...)

;; Filter elements matching a predicate
;; (define (filter [pred : (Fn [a] Bool)] [xs : (List a)]) : (List a) ...)

;; Fold left
;; (define (fold [init : b] [f : (Fn [b a] b)] [xs : (List a)]) : b ...)

;; Length
;; (define (length [xs : (List a)]) : Int ...)
